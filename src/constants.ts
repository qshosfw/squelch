export const FIRMWARE_REPOS = [
    "egzumer/uv-k5-firmware-custom",
    "fagci/uv-k5-firmware-fagci-mod",
    "k5_quansheng_firmware"
];

// Protocol Constants (moved from protocol.ts or duplicates if needed, but we'll stick to importing from protocol.ts)
